{
  "name": "browser-eye-tracking-examples",
  "version": "1.0.0",
  "description": "Examples for browser-eye-tracking library",
  "type": "module",
  "scripts": {
    "serve": "bun run --hot server.ts",
    "dev": "bun run --hot server.ts"
  },
  "dependencies": {
    "browser-eye-tracking": "file:../",
    "typescript": "^5"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}