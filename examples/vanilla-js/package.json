{
  "name": "web-eye-tracking-recorder-examples",
  "version": "1.0.0",
  "description": "Examples for web-eye-tracking-recorder library",
  "type": "module",
  "scripts": {
    "build-lib": "cd ../.. && bun run build",
    "serve": "bun run build-lib && bun run --hot server.ts",
    "dev": "bun run build-lib && bun run --hot server.ts",
    "build": "bun build demo.ts --outdir dist --target browser --format esm"
  },
  "dependencies": {
    "typescript": "^5"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}